<view:DocumentViewBase x:Class="DevelopmentInProgress.ExecutorMonitor.Wpf.View.MonitorView"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:view="clr-namespace:DevelopmentInProgress.Origin.View;assembly=DevelopmentInProgress.Origin" 
                       xmlns:messaging="clr-namespace:DevelopmentInProgress.WPFControls.Messaging;assembly=DipWpfControls"
                       mc:Ignorable="d" >
    <DockPanel LastChildFill="True">
        <GroupBox Header="Subscribe to Run" DockPanel.Dock="Top" Margin="5">
            <DockPanel LastChildFill="True">
                <Label DockPanel.Dock="Left" Content="Server URI:" Margin="5"/>
                <TextBox DockPanel.Dock="Left" Text="{Binding ServerUri}" Margin="5" Width="200"/>
                <Label DockPanel.Dock="Left" Content="Run Id:" Margin="5"/>
                <TextBox DockPanel.Dock="Left" Text="{Binding RunId}" Margin="5" Width="50"/>
                <Button DockPanel.Dock="Left" Content="Subscribe" Command="{Binding SubscribeCommand}" Margin="5"/>
                <Button DockPanel.Dock="Left" Content="Run" Command="{Binding RunCommand}" Margin="5"/>
                <Label/>
            </DockPanel>
        </GroupBox>
        <messaging:MessagePanel HeaderBackground="Transparent"
                                HeaderText="Notifications"
                                Messages="{Binding Notifications}" 
                                ClearMessages="{Binding ClearNotificationsCommand}" 
                                IsExpanded="True"
                                Margin="5"/>
    </DockPanel>
</view:DocumentViewBase>
